# Correction dictionary for transcription
# Format: wrong text = correct text
# One correction per line, empty lines and lines starting with # are ignored
#
# Examples:
# teh = the
# recieve = receive
